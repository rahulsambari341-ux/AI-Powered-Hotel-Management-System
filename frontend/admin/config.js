// Deployment configuration for the staff admin dashboard.
// See ../config.js for the same pattern - edit this to point at your
// deployed backend's real HTTPS URL in production.
window.HOTEL_API_BASE = "http://localhost:8000";
